/** Pixel height of one hour row in the week grid. */
export const HOUR_HEIGHT_PX = 56

/** Grid starts at 06:00 (in minutes since midnight). */
export const GRID_START_MINUTE = 360

/** Grid ends at 23:00 (in minutes since midnight). */
export const GRID_END_MINUTE = 1380

/** Default snap increment (minutes) used when dragging/resizing tasks. */
export const DEFAULT_SNAP_MINUTES = 15

/** Snap increment used when creating a task from a click on an empty cell. */
export const CREATE_SNAP_MINUTES = 30

/** Minimum duration (minutes) a task can be resized down to. */
export const MIN_TASK_DURATION_MINUTES = 15

export const MINUTES_PER_DAY = 24 * 60
